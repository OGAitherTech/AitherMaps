const VERSION='3.1.0';
const $=id=>document.getElementById(id);const saved=JSON.parse(localStorage.getItem('aitherMapsSettings')||'{}');let settings={theme:saved.theme||'system',map:saved.map||'standard',scale:saved.scale!==false};let userMarker=null,selectedMarker=null,routeLine=null;
const layers={standard:L.tileLayer('https://{s}.tile.openstreetmap.org/{z}/{x}/{y}.png',{maxZoom:19,attribution:'© OpenStreetMap contributors'}),dark:L.tileLayer('https://{s}.basemaps.cartocdn.com/dark_all/{z}/{x}/{y}{r}.png',{maxZoom:20,attribution:'© OpenStreetMap © CARTO'}),satellite:L.tileLayer('https://server.arcgisonline.com/ArcGIS/rest/services/World_Imagery/MapServer/tile/{z}/{y}/{x}',{maxZoom:19,attribution:'Tiles © Esri'})};
const map=L.map('map',{zoomControl:false,center:[39.8283,-98.5795],zoom:4,layers:[layers.standard],tap:true});let scaleControl=L.control.scale({imperial:true,metric:true}).addTo(map);
function toast(msg){const t=$('toast');t.textContent=msg;t.classList.add('show');clearTimeout(toast.timer);toast.timer=setTimeout(()=>t.classList.remove('show'),2300)}function save(){localStorage.setItem('aitherMapsSettings',JSON.stringify(settings))}function applyTheme(){document.body.classList.toggle('dark',settings.theme==='dark');document.body.classList.toggle('light',settings.theme==='light');document.body.classList.toggle('system-dark',settings.theme==='system'&&matchMedia('(prefers-color-scheme: dark)').matches)}
function setLayer(name){if(!layers[name])return;Object.values(layers).forEach(l=>map.removeLayer(l));layers[name].addTo(map);settings.map=name;$('mapSetting').value=name;document.querySelectorAll('.choice').forEach(b=>b.classList.toggle('active',b.dataset.layer===name));save();toast(`${name[0].toUpperCase()+name.slice(1)} map enabled`)}function openPanel(){$('sidePanel').classList.add('open');$('sidePanel').setAttribute('aria-hidden','false')}function closePanel(){$('sidePanel').classList.remove('open');$('sidePanel').setAttribute('aria-hidden','true')}
function locate(){if(!navigator.geolocation)return toast('Location is not supported');toast('Finding your location…');navigator.geolocation.getCurrentPosition(pos=>{const{latitude,longitude}=pos.coords;map.setView([latitude,longitude],15);if(userMarker)userMarker.remove();userMarker=L.circleMarker([latitude,longitude],{radius:9}).addTo(map).bindPopup('<b>You are here</b>').openPopup();$('locationLabel').textContent=`${latitude.toFixed(3)}, ${longitude.toFixed(3)}`},()=>toast('Unable to access your location'),{enableHighAccuracy:true,timeout:10000,maximumAge:30000})}
function addRecent(place){let list=JSON.parse(localStorage.getItem('aitherMapsRecent')||'[]');list=[place,...list.filter(x=>x.lat!==place.lat||x.lon!==place.lon)].slice(0,10);localStorage.setItem('aitherMapsRecent',JSON.stringify(list))}function addFavorite(place){const list=JSON.parse(localStorage.getItem('aitherMapsFavorites')||'[]');if(!list.some(x=>x.lat===place.lat&&x.lon===place.lon))list.push(place);localStorage.setItem('aitherMapsFavorites',JSON.stringify(list));toast('★ Saved place')}
function showList(key,empty){const list=JSON.parse(localStorage.getItem(key)||'[]');if(!list.length)return toast(empty);$('searchResults').hidden=false;$('searchResults').innerHTML='';list.forEach(place=>{const b=document.createElement('button');b.className='result';b.innerHTML=`${key.includes('Favorites')?'★':'◷'} ${escapeHtml(place.name)}<small>${escapeHtml(place.address||'')}</small>`;b.onclick=()=>{map.setView([place.lat,place.lon],15);if(selectedMarker)selectedMarker.remove();selectedMarker=L.marker([place.lat,place.lon]).addTo(map).bindPopup(escapeHtml(place.address||place.name)).openPopup();$('searchResults').hidden=true}})}
async function search(q){if(!q.trim())return;$('searchResults').hidden=false;$('searchResults').innerHTML='<div class="result">Searching…</div>';try{const res=await fetch(`https://nominatim.openstreetmap.org/search?format=jsonv2&limit=8&q=${encodeURIComponent(q)}`,{headers:{Accept:'application/json'}});if(!res.ok)throw Error();const data=await res.json();if(!data.length){$('searchResults').innerHTML='<div class="result">No places found.</div>';return}$('searchResults').innerHTML='';data.forEach(place=>{const b=document.createElement('button');b.className='result';b.innerHTML=`${escapeHtml(place.display_name.split(',')[0])}<small>${escapeHtml(place.display_name)}</small>`;b.onclick=()=>selectPlace(place);$('searchResults').appendChild(b)})}catch(e){$('searchResults').innerHTML='<div class="result">Search failed. Check your connection.</div>'}}
function selectPlace(place){const lat=+place.lat,lon=+place.lon,name=place.display_name.split(',')[0];addRecent({name,address:place.display_name,lat,lon});map.setView([lat,lon],15);if(selectedMarker)selectedMarker.remove();selectedMarker=L.marker([lat,lon]).addTo(map).bindPopup(`${escapeHtml(place.display_name)}<br><button id="savePlace" class="popup-save">★ Save place</button><button id="routeHere" class="popup-save">➤ Route here</button>`).openPopup();selectedMarker.on('popupopen',()=>{$('savePlace')?.addEventListener('click',()=>addFavorite({name,address:place.display_name,lat,lon}));$('routeHere')?.addEventListener('click',()=>{$('routePanel').hidden=false;$('toInput').value=place.display_name;$('fromInput').focus()})});$('locationLabel').textContent=place.display_name.split(',').slice(0,2).join(',');$('searchResults').hidden=true;$('searchInput').value=place.display_name}
function escapeHtml(s){const d=document.createElement('div');d.textContent=s;return d.innerHTML}
async function geocode(q){const res=await fetch(`https://nominatim.openstreetmap.org/search?format=jsonv2&limit=1&q=${encodeURIComponent(q)}`,{headers:{Accept:'application/json'}});if(!res.ok)throw Error();const data=await res.json();if(!data[0])throw Error();return[+data[0].lon,+data[0].lat]}
async function buildRoute(from,to){$('routeStatus').textContent='Calculating route…';if(routeLine)routeLine.remove();try{const a=await geocode(from),b=await geocode(to);const res=await fetch(`https://router.project-osrm.org/route/v1/driving/${a[0]},${a[1]};${b[0]},${b[1]}?overview=full&geometries=geojson&steps=true`);if(!res.ok)throw Error();const data=await res.json();if(data.code!=='Ok'||!data.routes?.length)throw Error();const r=data.routes[0];routeLine=L.geoJSON(r.geometry,{weight:6}).addTo(map);map.fitBounds(routeLine.getBounds(),{padding:[55,130]});const miles=(r.distance/1609.344).toFixed(1),mins=Math.max(1,Math.round(r.duration/60));const steps=r.legs.flatMap(x=>x.steps||[]).slice(0,5).map(s=>escapeHtml(s.name||'Continue')).join(' → ');$('routeStatus').innerHTML=`<strong>${miles} mi</strong> · <strong>${mins} min</strong><br><span>${steps||'Follow the highlighted route.'}</span>`;toast('Route ready')}catch(e){$('routeStatus').textContent='Could not calculate this route. Try different locations.';toast('Route unavailable')}}
$('searchForm').onsubmit=e=>{e.preventDefault();search($('searchInput').value)};$('clearSearch').onclick=()=>{$('searchInput').value='';$('searchResults').hidden=true;$('searchInput').focus()};$('menuBtn').onclick=openPanel;$('closePanel').onclick=closePanel;$('locateBtn').onclick=locate;$('zoomIn').onclick=()=>map.zoomIn();$('zoomOut').onclick=()=>map.zoomOut();$('layersBtn').onclick=()=>{$('layerCard').hidden=!$('layerCard').hidden};$('routeBtn').onclick=()=>{$('routePanel').hidden=false;$('fromInput').focus()};$('closeRoute').onclick=()=>$('routePanel').hidden=true;document.querySelectorAll('.choice').forEach(b=>b.onclick=()=>setLayer(b.dataset.layer));document.querySelectorAll('.menu-item').forEach(b=>b.onclick=()=>{closePanel();const a=b.dataset.action;if(a==='locate')locate();if(a==='layers')$('layerCard').hidden=false;if(a==='favorites')showList('aitherMapsFavorites','No saved places yet');if(a==='recent')showList('aitherMapsRecent','No recent searches');if(a==='settings')$('settings').hidden=false});$('routeForm').onsubmit=e=>{e.preventDefault();const f=$('fromInput').value.trim(),t=$('toInput').value.trim();if(!f||!t)return toast('Enter a start and destination');buildRoute(f,t)};
$('closeSettings').onclick=()=>$('settings').hidden=true;$('themeSetting').value=settings.theme;$('mapSetting').value=settings.map;$('scaleSetting').checked=settings.scale;$('themeSetting').onchange=e=>{settings.theme=e.target.value;applyTheme();save()};$('mapSetting').onchange=e=>setLayer(e.target.value);$('scaleSetting').onchange=e=>{settings.scale=e.target.checked;scaleControl.remove();if(settings.scale)scaleControl=L.control.scale({imperial:true,metric:true}).addTo(map);save()};$('clearData').onclick=()=>{if(confirm('Clear saved places and recent searches?')){localStorage.removeItem('aitherMapsFavorites');localStorage.removeItem('aitherMapsRecent');toast('Saved data cleared')}};$('forceUpdate').onclick=async()=>{if(confirm('Force update Aither Maps now?')){if('caches'in window){const keys=await caches.keys();await Promise.all(keys.map(k=>caches.delete(k)))}localStorage.removeItem('aitherMapsSettings');location.reload()}};document.addEventListener('click',e=>{if(!e.target.closest('.search-wrap'))$('searchResults').hidden=true;if(!e.target.closest('#layerCard')&&!e.target.closest('#layersBtn')&&!e.target.closest('[data-action="layers"]'))$('layerCard').hidden=true});applyTheme();setLayer(settings.map);if(!settings.scale)scaleControl.remove();if('serviceWorker'in navigator)navigator.serviceWorker.register('sw.js').catch(()=>{});
